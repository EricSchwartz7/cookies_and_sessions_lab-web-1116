<%= form_tag('/') do %>
  <label>Product:</label>
  <%= text_field_tag(:product) %>
  <%= submit_tag('add to cart') %>
<% end %>

<p>Your cart includes:
  <% @cart.each do |item| %>
    <%= item %>
  <% end %>
</p>
